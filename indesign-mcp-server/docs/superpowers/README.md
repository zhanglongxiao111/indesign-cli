# Superpowers 工作流文档

这里放方案和计划类过程文档。

| 目录 | 用途 |
| ---- | ---- |
| `specs/` | 方案设计、边界分析、备选方案 |
| `plans/` | 实施计划、阶段拆分、验证清单 |

`specs` 和 `plans` 是工作过程材料。落地后的长期结论，需要同步到 `AGENTS.md`、`docs/技术决策/` 或相关当前文档。
