## 簇 ID

FC-20260904-04　文案与文档快通道包（单项未达建簇阈值，合并处理）

按 `docs/技术决策/2026-07-06-agent反馈闭环流程.md` 快通道口径：只改文案、description、Skill 提示，不改工具行为。最后一项（draft 标记）超出快通道，单列决策。

## 证据与建议

| # | 现象 | 证据 | 建议 |
| --- | --- | --- | --- |
| 1 | `SEMANTIC_TOKEN_UNKNOWN` 不给候选 | 2 session / 2 台工位；Agent 编造 `body-text`、`content` | `audit-authoring.js` message 附本 kind 已登记 token 列表（`collectKnownSemanticTokens` 现成），超过 20 个时给最近似的 5 个 |
| 2 | `AUTHOR_PAGE_SECTION_INVALID` 不说找到几个、要什么选择器 | 1 session，13 个页面文件同时报 | message 改为"found N page sections in <file>; expected exactly one `<section class="page">` or `<section data-page>`" |
| 3 | `AUTHOR_STYLE_BUCKET_MISSING` 措辞是"Recommended"，strict 下却升级为 error 拦构建 | 1 session，4 条 | 二选一：strict 下不提升该码；或改措辞为"required under strict" |
| 4 | 文本溢出只报 `content.text differs` | 08-31 作者工位 6 个 item actual 都是 expected 的前缀（末词被截）；0819 审计的 p6-el19 同类 | `compareText` 检测 actual 是 expected 前缀时改码 `FORWARD_TEXT_OVERSET` 并给 hint"文本框容不下，加大框或缩短文本"；不改比对结果，只改码和提示 |
| 5 | `feedback report` 零使用 | 全历史 1 条探针，08-19 起 0 条；`SKILL.md` 与全部 `references/*.md` 没有出现 feedback 一词 | `failure-handling.md` 末尾加一段：遇到 `runtime_error`、文档说不清、工具缺口时执行 `indesign-cli feedback report --code <TOOL_GAP|DOC_UNCLEAR|ERROR_MESSAGE_USELESS|SCHEMA_CONFUSING> --note "<不含客户信息>"` |
| 6 | draft 成品被当正式交付 | `DESKTOP-HI43JGC` 08-26/08-27 三次构建都是 draft（`plugin_metrics` 只有 `verify_ms` 无 `fidelity_gate_ms`），outDir 在 `04-PRESENTATION\` 交付目录下 | 文档：Skill 已写"draft 不能作为正式成品"，但人看不到。**决策项（非快通道）**：draft 模式给 `outputBaseName` 追加 `-draft` 后缀，或在 outDir 写 `DRAFT-未验证.txt` 标记，让状态在文件名上可见 |

## 承接测试

- 错误信息类：1-4 各加单元测试断言 message / code。
- 文档类：`test_published_skill_*` 增加 grep 断言 `failure-handling.md` 含 `feedback report`。
- 第 6 项若采纳：`html.build_indesign` 契约测试断言 draft 模式产物文件名。
