# indesign-cli 遥测周报 2026-09-04

> 分析窗口：2026-08-19（0819 摩擦修复上线日）至 2026-09-04。遥测最后一条事件是 2026-09-01。
> 聚合文件：`\\daga-nas5\sa-ai-app\feedback-reports\indesign-cli-telemetry\reports\aggregate_2026-09-04.json`（全量 72 文件 / 677 事件 / 71 session）。
> 本期同时抽查了 NAS 上 `feedback-reports\<姓名>\` 的 SA 客户端反馈（08-19 起 60+ 条），以及同事工位 outDir 里归档的失败报告。

## 一句话结论

同事用起来"最终都能出成品"，但**每个作者包平均要被门禁拦 2-3 次**，其中两类拦截是工具自身的问题：表格保真门禁在当前版本必失败（同事的应对是把表格全部删掉），网格门禁被整包 `data-id-grid-ignore` 绕过（门禁已无实际约束力）。另有一处导出失败在 40-90 秒构建后才报错，且文案是原始 JSON。三者都值得修，其余是文案和文档级快通道。

## 样本规模（08-19 起）

| 指标 | 数值 |
| --- | --- |
| session / 工具调用 / 失败 | 17 / 125 / 28 |
| 工位 | 5 台：作者本人 2 台（`LONGXIAO`、`BF-202306081035`，合计 80 次调用）、同事 2 台（`DESKTOP-HI43JGC` 25 次 / 6 session，`DESKTOP-1CJEQS8` 19 次 / 2 session）、1 台只跑了 `server.health` |
| 版本 | 全部 0.5.11 + html-indesign 0.2.10（launcher 自动升级生效；一台从 0.4.2 直升） |
| Agent 主动 feedback | 0 条（全历史仅 07-10 一条发布探针） |
| 客户端反馈目录 | 60+ 条，全部关于 SA 客户端画布/助手/素材库；没有一条同事反馈提到 indesign-cli |

**样本偏置提示**：真正的同事使用只有 2 台工位、8 个 session、6 个作者包。结论有代表性但不宜外推为"全员"。

## 北极星指标（按 0.5.11 切片）

| 指标 | 值 | 说明 |
| --- | --- | --- |
| `script.run` 使用率 | 0 / 15 session | 同事没有绕过插件走裸脚本，好信号 |
| 首次调用成功率 | 53% | 首次调用多为 `strict:true` lint，被拦是设计预期，不是缺陷 |
| 会话收尾质量 | 87% | 代理指标；同事的 8 个 session 全部以成功构建收尾 |
| 每 session 调用数 | 平均 7.7，最多 28 | |
| `server.health` | 8 / 8 命令成功 | 组件健康未记录（`not_recorded`），不能据此说环境健康 |
| 环境类错误 / 超时 | 0 / 0 | |

## 错误分布（08-19 起）

| error_code × tool_id | 次数 | session 数 | 性质 |
| --- | --- | --- | --- |
| `AUTHORING_LINT_FAILED` × `html.authoring_lint` | 13 | 6 | 门禁；其中 10 次以 `GRID_ALIGNMENT_OFF` 为主（46-381 条/次） |
| `FIDELITY_GATE_FAILED` × `html.build_indesign` | 8 | 7 | 门禁；2 次是表格（工具缺陷），其余是 `content.text` / `paragraphStyle` |
| `AUTHORING_LINT_FAILED` × `html.build_indesign` | 2 | 1 | 作者包结构错（`AUTHOR_PAGE_SECTION_INVALID` ×13、`SEMANTIC_TOKEN_UNKNOWN` ×3） |
| `INDESIGN_EXPORT_FAILED` | 2 | 2 | 目标 INDD 已在 InDesign 中打开，保存失败；文案是原始 JSON |
| `INSTRUCTIONS_VALIDATION_FAILED` | 1 | 1 | 引用了不存在的占位图 |
| `ARGS_FILE_NOT_FOUND` | 1 | 1 | 首次调用 args 文件路径错 |
| `OUTPUT_OUTSIDE_PROJECT` | 1 | 1 | 作者本人测试，outDir 指向兄弟目录 |

重试率：lint 失败后 14/14 重试、build 失败后 12/13 重试——门禁类失败的正常表现，不是盲目重试。

## 门禁真实成本

- 首次带保真门禁的构建（11 个作者包）：**5 个第一次就被拦**。其中 2 个是表格误判（簇 01），3 个是文本溢出/段落样式。
- 一次 `html.build_indesign` 中位 42 秒，最长 173 秒（37 页）；每被拦一次就是一次完整 InDesign 往返。
- 从第一次 lint 到成功构建：同事的作者包耗时 2-20 分钟，节奏可接受；黑箱期主要花在反复改 `GRID_ALIGNMENT_OFF`。

## 摩擦簇

| 簇 | 标题 | 通道 | 文档 |
| --- | --- | --- | --- |
| FC-20260904-01 | 表格保真门禁在 0.2.10 必失败，同事用"删表格"绕过 | 正常修复（html-indesign） | `摩擦簇_FC-20260904-01.md` |
| FC-20260904-02 | `GRID_ALIGNMENT_OFF` 严格门禁被整包 `data-id-grid-ignore` 绕过，报告不给偏移量 | 正常修复（html-indesign）+ 文档 | `摩擦簇_FC-20260904-02.md` |
| FC-20260904-03 | 目标 INDD 已打开导致 `INDESIGN_EXPORT_FAILED`，构建完才失败且文案是原始 JSON | 正常修复（html-indesign） | `摩擦簇_FC-20260904-03.md` |
| FC-20260904-04 | 文案与文档快通道包（token 候选、page section 细节、feedback 入口、溢出提示、draft 标记） | 快通道 | `摩擦簇_FC-20260904-04.md` |

## 上期（0819）修复复核

| 0819 项 | 本期观察 | 状态 |
| --- | --- | --- |
| `prepare-author-package.ps1` UNC/中文路径 | 未再出现手搓作者包；`ARGS_FILE_NOT_FOUND` 仅 1 次 | 关闭 |
| 双组装脚本混淆 / `BAD_CLI_ARGS` 定向提示 | 本期无 `BAD_CLI_ARGS` | 关闭 |
| lint/保真失败报告原地覆盖 | `*.failed-<时间戳>.json` 已在同事 outDir 落盘，本期分析全靠它 | 关闭，且已验证有用 |
| `HTML_ROLE_INFERRED` 噪音改归一化计数 | 同事包 `warningCount` 已为 0，`normalizedCount` 72-180 | 关闭 |
| 自动宽度标题右边豁免（A3） | `GRID_ALIGNMENT_OFF` 仍是 lint 失败第一大项，边缘以 `left/top` 为主 | 未解决根因，转簇 02 |
| `FIDELITY_GATE_FAILED` 文案带 expected/actual | 有效；但表格类差异 expected/actual 各 5-6 行整块贴出，Agent 看不出只是 header/默认样式之差 | 转簇 01 |

## 正面观察（不要改坏的地方）

- 门禁的"N of M share code X — one systemic cause"提示有效：381 条网格错误 2 分钟内一次修完。
- 同事全程零 `script.run`、零环境错误、零超时；launcher 自动升级把一台 0.4.2 工位无感带到 0.5.11。
- `error.details.partialArtifacts`、`intermediateDir` 这些结构化字段在失败报告里齐全，离线复盘不需要 InDesign。

## 建议优先级

1. **簇 01（表格）**：确定性缺陷、影响所有带表格的作者包、同事已经在用"删表格"规避——先修。三处改动都小。
2. **簇 03（INDD 已打开）**：构建前 1 秒能查出的问题现在要 90 秒后才报，文案还不可读。
3. **簇 02（网格）**：先做"报告带偏移量 + 统计豁免数"，再决定要不要降级为 warning；不要静默继续。
4. **簇 04**：全部是文案，一次 PR 打包。

## 数据备注

- 08-19 `BF-202306081035` 的 D0486 session 是 0819 审计本身；08-25 D0474 session 同样在作者工位。统计"同事使用"时已剔除。
- `DESKTOP-KJ2BESC` 09-01 只有一次 `server.health`，cwd 是 Codex 工作目录，疑似试装，不计入使用。
- 本期未观察到 `html.reverse_export` 调用。
