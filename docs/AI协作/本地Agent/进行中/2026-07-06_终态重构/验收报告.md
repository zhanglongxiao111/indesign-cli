# Task 6 终局验收报告

日期：2026-07-07

分支：`refactor/terminal-architecture`

## 结论

Task 6 审核 FAIL 项已完成修复并重新验证。没有进入 Task 7，没有合并分支。

## 数量对账

- Node-backed 工具：`150 = 114 classic + 30 hidden_handler + 6 advanced`。
- 当前 full runner 实时 catalog：`159 = 150 Node-backed + 7 cli + 1 cli.primitive + 1 script`。
- `src/core/indesign-tool-registry.json`：`tool_count = 150`。
- golden manifest：`classicListTools = 114`，`advancedListTools = 6`，`cliDiscoverableTools = 150`，`schemaNetNewWhitelist = 9`，`skips = 0`。

## 根因与修复

1. `script.run` 返回形状：
   - 根因：CLI 解析层还保留 `data.result_json` 兼容投影，测试又断言 JSON 返回位于 `data.parsed.result`。
   - 修复：删除 `result_json` 兼容投影；权威入口统一为 `data.parsed`。JSX 返回 JSON 对象字符串时直接读取 `data.parsed.<field>`；普通文本仍读取 `data.parsed.result`。
   - 同步：`test_full_e2e.py`、`test_catalog_router.py`、`agent-harness/INDESIGN.md`、`agent-harness/cli_anything/indesign/README.md`、`skills/indesign-cli/SKILL.md`。

2. 非 offline `--full` 素材缺失：
   - 根因：`ensureInventoryAndAssets()` 只在 `--offline` 时准备 assets，但 `--full` 后续阶段一定会放置图片。
   - 修复：需要素材的 runner phase 统一先执行 `assets` 准备；当前项目只有 seed assets 通道，没有新增下载或 fallback 分支。

3. `NO_ACTIVE_DOCUMENT`：
   - 当前工作树未复现审核中的 `layer.create_layer` / `page.zoom_to_page` 文档状态丢失。
   - 已用 fresh run 验证 `main_deck_setup`、`--full --offline`、`--full` 均通过；两个原失败点均已覆盖。

4. legacy token：
   - 根因：`scripts/check_architecture.mjs` 仍保留白名单逻辑，活动脚本/源码中也有旧路径证据注释。
   - 修复：删除检查器白名单；legacy token 用拼接构造避免检查器自命中；清理 `scripts/migration/record_golden.mjs` 和 `src/tools/spread/placement.js` 活动命中。

## Contract Diff 确认

- baseline count：150。
- current Node-backed count：150。
- missing：0。
- extra：0。
- contract 布尔字段 diff：0。
- source diff：9 个，全部为计划内 `switch_only_no_cli_schema -> hidden_handler`，对应 9 个补写 schema 工具。

核对字段：`domain`、`name`、`needs_indesign`、`requires_active_document`、`mutates_document`、`writes_filesystem`、`destructive`。

## 9 个补写 Schema Review 记录

| 工具 | source | properties | required | 处理 |
| ---- | ------ | ---------- | -------- | ---- |
| `document.preflight_document` | `hidden_handler` | 2 | 无 | 已在 artifact 存在，contract diff 仅 source 变化 |
| `document.data_merge` | `hidden_handler` | 4 | `dataSource` | 已在 artifact 存在，contract diff 仅 source 变化 |
| `document.get_document_xml_structure` | `hidden_handler` | 2 | 无 | 已在 artifact 存在，contract diff 仅 source 变化 |
| `document.export_document_xml` | `hidden_handler` | 3 | `filePath` | 已在 artifact 存在，contract diff 仅 source 变化 |
| `document.save_document_to_cloud` | `hidden_handler` | 2 | `cloudName` | 已在 artifact 存在，contract diff 仅 source 变化 |
| `document.open_cloud_document` | `hidden_handler` | 1 | `cloudDocumentId` | 已在 artifact 存在，contract diff 仅 source 变化 |
| `document.validate_document` | `hidden_handler` | 4 | 无 | 已在 artifact 存在，contract diff 仅 source 变化 |
| `document.cleanup_document` | `hidden_handler` | 4 | 无 | 已在 artifact 存在，contract diff 仅 source 变化 |
| `spread.place_xml_on_spread` | `hidden_handler` | 6 | `spreadIndex`, `xmlElementName` | 已在 artifact 存在，contract diff 仅 source 变化 |

## Golden Diff 摘要

- 本次未改写 `golden/` 文件。
- golden `skip_list.json` 为 `[]`。
- fresh D offline run：`task6-acceptance-full-offline-final-20260707` 通过，未沿用旧 golden run。
- fresh D offline coverage：`passed = 145`，`failed = 0`，`blocked = 0`，`expected_failure_passed = 1`，`pending = 13`。
- `expected_failure_passed`：`book.print_book`，原因是避免发送真实打印任务，仅覆盖安全 missing-file guard。

## Skip 清单处理结果

- golden skip 清单：0 项。
- coverage pending 13 项不是 golden skip：
  - CLI/primitive：`tool.batch`、`server.setup`、`session.doctor`、`feedback.report`。
  - 9 个补写 schema 工具：`document.cleanup_document`、`document.data_merge`、`document.export_document_xml`、`document.get_document_xml_structure`、`document.open_cloud_document`、`document.preflight_document`、`document.save_document_to_cloud`、`document.validate_document`、`spread.place_xml_on_spread`。

## 真实门禁结果

已通过：

```powershell
node tests\real-e2e\run-architecture-presentation.mjs --full --offline --run-id task6-acceptance-full-offline-final-20260707
```

```powershell
node tests\real-e2e\run-architecture-presentation.mjs --full --run-id task6-acceptance-full-20260707
```

```powershell
$env:INDESIGN_E2E='1'; python -m pytest agent-harness\cli_anything\indesign\tests\test_full_e2e.py -q
```

```powershell
python -m pytest agent-harness\cli_anything\indesign\tests\test_catalog_router.py agent-harness\cli_anything\indesign\tests\test_cli_entrypoint.py agent-harness\cli_anything\indesign\tests\test_paths_envelope.py -q
```

```powershell
node scripts\check_architecture.mjs
```

```powershell
node tests\index.js --required
```

```powershell
rg -n "InDesignMCPServer|src/handlers/|src/types/|hidden_handler_schemas|domains\.py|TOOL_MAP" src agent-harness tests scripts
```

补充说明：

- `python -m pytest agent-harness\cli_anything\indesign\tests\test_core.py -q` 未运行成功，因为当前仓库不存在 `test_core.py`。
- 语法检查已覆盖：`node --check tests\real-e2e\run-architecture-presentation.mjs`、`node --check scripts\check_architecture.mjs`、`node --check scripts\migration\record_golden.mjs`、`node --check src\tools\spread\placement.js`。
