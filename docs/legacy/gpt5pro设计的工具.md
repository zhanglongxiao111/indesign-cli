### 方案A（按“建筑汇报模板 + 必要灵活操作”精简，≤40 个）

> 说明：该方案将原始 120+ InDesign MCP 工具整合为模板化页面生产 + 少量通用原子操作，便于 Agent 以最少调用完成建筑汇报常见版式。表中“合并自”列标注了主要来源工具名称，便于迁移与适配。所有工具统一返回结构：  
> `{ ok: boolean, message: string, warnings?: string[], data?: object, requestId: string }`（除非另有补充）。原始工具清单见 InDesign MCP Server 文档。

| 工具名                    | 用途 / 智能提示（给 Agent）                                          | 主要 JSON 字段（校验与枚举）                                                                                                                                                                                                                                                                                                             | 返回 / 反馈（核心 `data` 字段）                                                                                                           | 合并自（原工具）                                                                                                             |
| ---------------------- | ----------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| doc.create             | 新建汇报文档；设定尺寸、页数、出血与页边距；常用于项目开局或重置                            | `width, height`(mm, number)；`pages`(int, ≥1)；`facingPages`(bool)；`orientation`(enum: portrait/landscape)；`bleed`{top/bottom/inside/outside}(mm)；`margins`{top/right/bottom/left}(mm)                                                                                                                                          | `data`: `{documentId, pageCount, size, bleed, margins}`                                                                         | `create_document`                                                                                                    |
| doc.open               | 打开既有 INDD                                                   | `filePath`(string)                                                                                                                                                                                                                                                                                                            | `data`: `{documentId, path}`                                                                                                    | `open_document`                                                                                                      |
| doc.save               | 保存（可另存）                                                     | `filePath?`(string)                                                                                                                                                                                                                                                                                                           | `data`: `{path, savedAt}`                                                                                                       | `save_document`                                                                                                      |
| doc.close              | 关闭当前文档                                                      | 无                                                                                                                                                                                                                                                                                                                             | `data`: `{closed: true}`                                                                                                        | `close_document`                                                                                                     |
| doc.export             | 导出 PDF/图像/EPUB/打包（统一一条）                                     | `type`(enum: pdf/images/epub/package)；`options`(object，随类型变化：PDF→`quality`、`includeMarks/bleed`、`pages`；Images→`format`、`resolution`、`quality`、`pages`；Package→`includeFonts/links/profiles` 等)；`outputPath`(string)                                                                                                          | `data`: `{type, outputPath, pagesExported?, packageDir?}`                                                                       | `export_pdf / export_images / export_epub / package_document`                                                        |
| doc.info.get           | 获取文档概要、颜色与样式计数                                              | 无                                                                                                                                                                                                                                                                                                                             | `data`: `{documentId, pageCount, styles: {paragraph, character, object}, swatches, layers}`                                     | `get_document_info / get_document_styles / list_color_swatches / list_layers`                                        |
| doc.pref.set           | 批量设置文档偏好（布局、网格、基线）                                          | `layout?`、`grid?`、`baseline?`（字段与原 InDesign 术语一致）                                                                                                                                                                                                                                                                             | `data`: `{layout, grid, baseline}`                                                                                              | `set_document_preferences / set_document_layout_preferences / set_document_grid_settings`                            |
| style.define           | 定义段落 / 字符 / 对象样式（统一）                                        | `type`(enum: paragraph/character/object)；`name`(string)；`props`(object：如 paragraph→`fontFamily,fontSize,leading,alignment,spaceBefore/After,textColor`；object→`fillColor,strokeColor,strokeWeight,cornerRadius,opacity`)                                                                                                      | `data`: `{styleType, name}`                                                                                                     | `create_paragraph_style / create_character_style / create_object_style`                                              |
| style.apply            | 应用样式到目标文本/对象                                                | `type`(enum: paragraph/character/object)；`target`:{`frameId?`,`pageIndex?`,`itemIndex?`}；文本时可带 `range?`(start,end) 或 `paragraphIndex?`                                                                                                                                                                                        | `data`: `{applied:true, styleName, target}`                                                                                     | `apply_paragraph_style / apply_character_style / apply_object_style`                                                 |
| swatch.create          | 新建颜色色板                                                      | `name`；`model`(enum: rgb)；`rgb`{r,g,b}(0–255)                                                                                                                                                                                                                                                                                 | `data`: `{swatchName}`                                                                                                          | `create_color_swatch`                                                                                                |
| swatch.list            | 列出所有色板                                                      | 无                                                                                                                                                                                                                                                                                                                             | `data`: `{swatches:[...]}`                                                                                                      | `list_color_swatches`                                                                                                |
| template.apply_page    | **模板页生成器**：一次创建“封面/效果图/分析/目录/指标/时间线/致谢”等标准页面（含占位框、标题、网格、背景） | `pageType`(enum: `cover / full_image / gallery_2/3/4 / analysis_2col / analysis_3col / site_context / massing_steps / circulation / sunlight / wind / viewshed / metrics_table / timeline / credits`)；`pageInsert`(enum: after/before/append, with `referencePage`)；`theme?`(引用 `template.set_theme` 的键)；`options?`(如徽标/背景开关) | `data`: `{pageIndex, template: {pageType, regionKeys:[...]}, regionMap:{key→frameId}}`                                          | （整合多个页面与形状/文本创建类工具）                                                                                                  |
| template.bulk_apply    | 批量生成同类模板页                                                   | `pageType`；`count`(int,1–50)；`startAt?`                                                                                                                                                                                                                                                                                       | `data`: `{pages:[{pageIndex, regionKeys}...]}`                                                                                  | 同上                                                                                                                   |
| template.update_region | 用占位键写入内容（文本/图片/表格）                                          | `pageIndex`；`regionKey`；`content`(union: `text{string,style?}` / `image{filePath,fitMode}` / `table{data[][],header?}`)                                                                                                                                                                                                       | `data`: `{regionKey, frameId, applied}`                                                                                         | `create_text_frame / place_image / create_table / populate_table`                                                    |
| template.set_theme     | 设定模板主题（字体、字号层级、色板、栅格间距）                                     | `name`；`typography`(H1/H2/Body/Caption 等)；`colors`(主色/辅色)；`spacing`(mm)                                                                                                                                                                                                                                                       | `data`: `{themeName}`                                                                                                           | （基于样式/色板/网格工具整合）                                                                                                     |
| page.add               | 新增页面（可指定位置）                                                 | `position`(enum: before/after/end)；`referencePage?`(int)                                                                                                                                                                                                                                                                      | `data`: `{pageIndex}`                                                                                                           | `add_page`                                                                                                           |
| page.delete            | 删除页面                                                        | `pageIndex`                                                                                                                                                                                                                                                                                                                   | `data`: `{deleted:true}`                                                                                                        | `delete_page`                                                                                                        |
| page.duplicate_or_move | 复制或移动页面                                                     | `action`(enum: duplicate/move)；`pageIndex`；`position`；`referencePageIndex?`                                                                                                                                                                                                                                                   | `data`: `{newPageIndex}`                                                                                                        | `duplicate_page / move_page`                                                                                         |
| page.guides            | 建立页面参考线与栅格                                                  | `pageIndex`；`rows, columns`(int)；`rowGutter, columnGutter`(mm)；`fitMargins`(bool)；`removeExisting?`                                                                                                                                                                                                                           | `data`: `{guideCount}`                                                                                                          | `create_page_guides`                                                                                                 |
| page.set_background    | 一键设背景（创建全幅矩形）                                               | `pageIndex`；`backgroundColor`(swatchName or rgb)；`opacity?`                                                                                                                                                                                                                                                                   | `data`: `{frameId}`                                                                                                             | `set_page_background / create_rectangle`                                                                             |
| master.manage          | 主版面管理（创建/复制/应用/删除/参考线）                                      | `action`(enum: create/duplicate/apply/delete/guides)；`name`/`masterName`；`pageRange?`；`guides?`                                                                                                                                                                                                                               | `data`: `{masterName, appliedPages?}`                                                                                           | `create_master_spread / duplicate_master_spread / apply_master_spread / delete_master_spread / create_master_guides` |
| textframe.upsert       | 新建或更新文本框（智能合并）                                              | **创建**：`pageIndex,x,y,width,height`；**更新**：`frameId` 或 `pageIndex+frameIndex`；通用：`content?`、`fontSize?`、`fontName?`、`textColor?`、`alignment?`、`paragraphStyle?`                                                                                                                                                               | `data`: `{frameId, edited:true}`                                                                                                | `create_text_frame / edit_text_frame`                                                                                |
| text.find_replace      | 文档级查找替换                                                     | `findText, replaceText`；`caseSensitive?`；`wholeWord?`；`useRegex?`                                                                                                                                                                                                                                                             | `data`: `{replacedCount}`                                                                                                       | `find_text_in_document / find_replace_text`                                                                          |
| table.upsert           | 创建或填充表格                                                     | 创建：`pageIndex,x,y,width,height,rows,columns,headerRows?,headerColumns?`；填充：`tableId` 或 `tableIndex`，`data[][]`                                                                                                                                                                                                                | `data`: `{tableId, cellsUpdated}`                                                                                               | `create_table / populate_table`                                                                                      |
| image.place            | 放置图片（页或跨页；自动适配）                                             | `scope`(enum: page/spread)；`pageIndex                                                                                                                                                                                                                                                                                         | spreadIndex`；`filePath`；`x,y,width?,height?`；`fitMode`(enum: none/fit-content/fit-frame/fill-frame)；`linkImage?`(bool)；`scale?` | `data`: `{frameId, linkPath}`                                                                                        |
| object.draw            | 快速绘制图形                                                      | `shape`(enum: rectangle/ellipse/polygon)；`pageIndex,x,y,width,height`；`sides?`；`fillColor?`；`strokeColor?, strokeWidth?`；`cornerRadius?`                                                                                                                                                                                      | `data`: `{itemId}`                                                                                                              | `create_rectangle / create_ellipse / create_polygon`                                                                 |
| item.transform         | 变换页面项目（移动/缩放/旋转/透明）                                         | `target`:{`itemId` 或 `pageIndex+itemIndex`}；`move?`{dx,dy}；`resize?`{width?,height?}；`rotate?`(deg)；`opacity?`(0–100)；`anchor?`                                                                                                                                                                                               | `data`: `{itemId, bbox}`                                                                                                        | `move_page_item / resize_page_item / reframe_page / set_page_item_properties`                                        |
| item.style             | 改变对象外观或套用对象样式                                               | `target`；`fillColor?, strokeColor?, strokeWeight?, cornerRadius?` 或 `objectStyleName?`                                                                                                                                                                                                                                        | `data`: `{itemId, styled:true}`                                                                                                 | `apply_object_style / set_page_item_properties`                                                                      |
| item.group             | 组合/解组/增删组内成员                                                | `action`(enum: group/ungroup/add/remove)；`pageIndex`；`itemIds` 或 `itemIndices[]`；`groupId?`                                                                                                                                                                                                                                   | `data`: `{groupId, members}`                                                                                                    | `create_group / create_group_from_items / ungroup / add_item_to_group / remove_item_from_group / list_groups`        |
| layer.manage           | 图层管理（新建/激活/显示隐藏/锁定/列出）                                      | `action`(enum: create/set_active/list/update)；`name?`；`visible?, locked?, color?`                                                                                                                                                                                                                                             | `data`: `{layerName, state}`                                                                                                    | `create_layer / set_active_layer / list_layers`                                                                      |
| spread.manage          | 跨页管理（列出/复制/移动/删除/属性/参考线）                                    | `action`(enum: list/duplicate/move/delete/properties/guides)；`spreadIndex?`；`position?`                                                                                                                                                                                                                                       | `data`: `{spreads                                                                                                               | spreadIndex                                                                                                          |
| select.by_summary      | 通过页面/跨页内容摘要选择项目（便于 Agent 先读后改）                              | `scope`(enum: page/spread)；`index`；`filter?`(type: text/image/shape, styleName?, label?)                                                                                                                                                                                                                                      | `data`: `{items:[{itemId,type,bbox,style}]}`                                                                                    | `get_page_content_summary / get_spread_content_summary / list_page_items`                                            |
| hyperlink.create       | 创建文本超链接（如跳目录）                                               | `pageIndex, frameId? or findText`；`destination`(url/pageIndex)；`linkType`                                                                                                                                                                                                                                                     | `data`: `{hyperlinkId}`                                                                                                         | `create_document_hyperlink`                                                                                          |
| search.assets          | 扫描并返回文档链接资源状态                                               | `includeMissing?`(bool)                                                                                                                                                                                                                                                                                                       | `data`: `{links:[{path,status}]}`                                                                                               | `get_image_info`（扩展用法）                                                                                               |
| help.tools             | 返回当前服务端支持的工具列表与 JSON schema（便于 Agent 自检）                    | `tool?`                                                                                                                                                                                                                                                                                                                       | `data`: `{tools:[{name, schema, examples}]}`                                                                                    | `help`                                                                                                               |
| script.run             | 兜底：运行 ExtendScript/JSX 片段（仅在无等价工具时使用）                       | `code`(string)                                                                                                                                                                                                                                                                                                                | `data`: `{stdout, stderr?}`                                                                                                     | `execute_indesign_code`                                                                                              |

> 合计：**33** 个工具（≤40）

---

### 方案B（“组件 / 意图驱动”精简——更通用、可复用，≤40 个，推荐）

> 说明：该方案不以“整页模板”为中心，而以“组件与配方（Recipe）”抽象版式；Agent 通过查询-放置-绑定数据的流水线快速搭建页面。对复杂/特殊页更友好，可组合度高。统一返回结构同上；原始工具映射见“合并自”。

| 工具名                                                          | 用途 / 智能提示（给 Agent）                    | 主要 JSON 字段（校验与枚举）                                                                                         | 返回 / 反馈（核心 `data` 字段）                                       | 合并自（原工具）                                                                       |
| ------------------------------------------------------------ | ------------------------------------- | --------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- | ------------------------------------------------------------------------------ |
| project.new                                                  | 新建文档（同 A-`doc.create`）                | 同上                                                                                                        | 同上                                                          | `create_document`                                                              |
| project.open / project.save / project.close / project.export | 文档打开/保存/关闭/导出（合并归类）                   | 同 A 方案对应字段                                                                                                | 同 A                                                         | `open/save/close/export_*`                                                     |
| theme.set                                                    | 设置全局主题 Token（字体、字号层级、色板、间距、基线网格）      | `typography, colors, spacing, baseline`                                                                   | `{theme}`                                                   | `create_*_style / create_color_swatch / set_document_grid_settings`            |
| grid.use                                                     | 应用常用栅格配方（如 12 列/24 行、黄金比间距等）          | `preset`(enum: `12c-24r / 8c-baseline / free`)；或 `custom`{rows,columns,rowGutter,columnGutter,fitMargins} | `{guides:{rows,columns}}`                                   | `create_page_guides / create_spread_guides`                                    |
| recipe.apply                                                 | **页面配方**：以“组件清单 + 约束”声明一页内容（比整页模板更灵活） | `pageInsert`；`components:[{type, key, rect(x,y,w,h), props, bind?}]`（`type` 见下若干组件）                       | `{pageIndex, components:[{key, componentId, regionSlots}]}` | （由多种创建/放置工具组合而来）                                                               |
| component.place                                              | 放置一个组件实例                              | `type`；`pageIndex`；`rect`；`props?`；`label?`                                                               | `{componentId, slots}`                                      | 同上                                                                             |
| component.update                                             | 更新组件（含内容与样式 Token）                    | `componentId`；`slot`(如 `title/image_1/caption`)；`value`(text/image/table)                                 | `{componentId, slot, applied}`                              | 同上                                                                             |
| component.types                                              | 列出可用组件类型与所含插槽                         | 无                                                                                                         | `{types:[{type, slots, defaultProps}]}`                     | `help`                                                                         |
| selector.query                                               | 通过条件选择对象（比 index 更稳，推荐先标注 label）      | `scope`(page/spread/doc)；`where`{`type?`(text/image/shape/table), `label?`, `styleName?`}                 | `{items:[{itemId,type,label,bbox}]}`                        | `get_page_content_summary / list_page_items`                                   |
| assets.register                                              | 预登记素材键值（便于批量绑定）                       | `assets:[{key, filePath}]`                                                                                | `{registered:[key...]}`                                     | ——                                                                             |
| assets.place_batch                                           | 批量将素材按规则流入组件槽位                        | `mapping:[{componentId, slot, assetKey, fitMode}]`                                                        | `{placed:n}`                                                | `place_image`                                                                  |
| text.style.apply                                             | 以“角色”套用文本样式（H1/H2/Body/Caption）       | `role`(enum)；`target`；`overrides?`                                                                        | `{applied:true}`                                            | `apply_paragraph_style / apply_character_style`                                |
| text.find_replace                                            | 文档级查找替换                               | 同 A                                                                                                       | 同 A                                                         | 同 A                                                                            |
| table.from_metrics                                           | 根据指标 JSON 生成表格（指标页/对比表）               | `pageIndex` 或 `componentId`；`metrics`(JSON)；`layout`(cols, header)                                        | `{tableId, rows, cols}`                                     | `create_table / populate_table`                                                |
| timeline.generate                                            | 里程碑/时间线组件生成                           | `pageIndex`；`events:[{time,label,desc?}]`；`style?`                                                        | `{componentId}`                                             | （文本+形状组合）                                                                      |
| diagram.make                                                 | **分析图组件**（统一：流线/日照/风/视线）占位+图例         | `type`(enum:`circulation/sunlight/wind/viewshed`)；`pageIndex`；`legend?`；`notes?`                          | `{componentId, slots:{diagram, legend, notes}}`             | （对象/文本/图层工具组合）                                                                 |
| callout.add                                                  | 添加批注/引出标注（箭头+文本）                      | `pageIndex`；`anchor`(x,y)；`text`；`style?`                                                                 | `{groupId, itemIds}`                                        | `create_rectangle/ellipse/polygon + set_page_item_properties + create_group`   |
| gallery.make                                                 | 图片墙组件（2/3/4 列）                        | `pageIndex`；`cols`(2–4)；`gutter`；`images?`                                                                | `{componentId, slots:[image_1..n]}`                         | `place_image`                                                                  |
| hero.cover                                                   | 封面组件（标题/副标题/项目信息/背景图）                 | `pageIndex`；`title, subtitle, meta?`；`bgImage?`                                                           | `{componentId, slots}`                                      | 同上                                                                             |
| layout.auto_balance                                          | 自动均衡当前页的若干组件（对齐/间距）                   | `pageIndex`；`componentIds[]`；`mode`(enum: grid/stack/justified)；`gutter`                                  | `{moved:n}`                                                 | `adjust_page_layout / align-distribute（由 set_* 派生）`                            |
| page.manage                                                  | 新增/删/复制/移动页面（合并）                      | `action`(add/delete/duplicate/move)；`pageIndex?`；`position?`；`reference?`                                 | `{pageIndex/newPageIndex}`                                  | `add_page / delete_page / duplicate_page / move_page`                          |
| page.background                                              | 背景与画布色（含透明度）                          | `pageIndex, backgroundColor, opacity?`                                                                    | `{frameId}`                                                 | `set_page_background`                                                          |
| layer.use                                                    | 创建/切换/更新图层（合并）                        | `action`(create/set_active/update/list)；`name?`；`visible?, locked?, color?`                               | `{layers or layerName}`                                     | `create_layer / set_active_layer / list_layers`                                |
| item.transform                                               | 变换/透明                                 | `target`；`move?/resize?/rotate?/opacity?`                                                                 | `{itemId,bbox}`                                             | `move_page_item / resize_page_item / set_page_item_properties`                 |
| item.group                                                   | 组操作                                   | `action`；`pageIndex`；`itemIds[]/groupId`                                                                  | `{groupId,members}`                                         | 同 A                                                                            |
| shape.draw                                                   | 形状（含多边形/箭头）                           | `shape`(rectangle/ellipse/polygon/arrow)；几何与样式                                                            | `{itemId}`                                                  | `create_rectangle/ellipse/polygon`                                             |
| image.place                                                  | 放图（单）                                 | 同 A                                                                                                       | 同 A                                                         | 同 A                                                                            |
| textframe.upsert                                             | 文本框（单）                                | 同 A                                                                                                       | 同 A                                                         | 同 A                                                                            |
| hyperlink.create                                             | 文内链接/目录跳转                             | `source`(frameId/findText)；`destination`                                                                  | `{hyperlinkId}`                                             | 同 A                                                                            |
| qc.lint                                                      | 预检导出质量（缺字库/断链/溢出文本）                   | `rules?`                                                                                                  | `{issues:[{type, target, severity, hint}]}`                 | `get_document_stories(includeOverset) / get_image_info / package_document(模拟)` |
| selector.label                                               | 给对象/组件打 label（便于后续选择）                 | `target`；`label`                                                                                          | `{itemId,label}`                                            | `set_page_item_properties（扩展）`                                                 |
| help.tools                                                   | 工具/组件 Schema 自说明                      | `name?`                                                                                                   | `{schema or list}`                                          | `help`                                                                         |
| script.run                                                   | ExtendScript 兜底                       | `code`                                                                                                    | `{stdout,stderr?}`                                          | `execute_indesign_code`                                                        |

> 合计：**28** 个工具（≤40）



下面给出基于你“**由人类用户先行设计模板，Agent 只做内容填写 + 少量原子化操作，ExtendScript + 人类兜底**”的新方案——**一套可投入生产的精简工具集（≤35 个）**。  
统一返回结构（除非单独声明）：

```json
{ "ok": true/false, "message": "string", "warnings": ["string"], "data": {…}, "requestId": "uuid" }
```

通用约定：`pageIndex` 0 基；长度/坐标单位 mm；颜色可用色板名或 `{r,g,b}`；`fitMode` 取 `none|fit-content|fit-frame|fill-frame`。原始工具来源参见 **InDesign MCP Server 120+ 工具文档**（本方案将其合并精简并保留关键字段）。

---

## A. 模板驱动核心（人类制作 → Agent 调用）

> **目标**：人类在 InDesign 中制作若干 .INDD 模板（含主版面、标注好 Slot 的文本框/图片框/表格框、样式与色板），Agent 只负责选择模板、映射 Slot、填充内容、导出成品；特殊情况用少量原子工具或 ExtendScript 兜底。

### A1. 模板生命周期（11 个）

| 工具名                     | 用途 / 智能提示（给 Agent）                                | 主要 JSON 字段（校验/枚举）                                                                                                    | 返回 / 反馈（`data` 核心）                             | 合并自（原工具）                                                |
| ----------------------- | ------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- | ------------------------------------------------------- |
| template.register       | **注册**人类制作的模板（指向 .indd 路径），解析并缓存 Slot Schema 与缩略图 | `name`(string)，`version`(string)，`filePath`(string)，`thumbnail?`(path)，`slotSchema?`([{`key`,`type`:`text            | image                                          | table`，`required?`，`target`:{`byLabel`}}])              |
| template.list           | 列出可用模板，支持按标签/用途过滤                                 | `q?`，`tags?`([])                                                                                                     | `{templates:[{templateId,name,version,tags}]}` | `help / view_document`                                  |
| template.preview        | 生成/读取模板缩略图或指定页预览图                                 | `templateId`，`pages?`(string 如 `1-3,7`)                                                                              | `{previews:[{page, imagePath}]}`               | `export_images`                                         |
| template.validate       | 校验模板完备性（Slot/样式/链接/字体）                            | `templateId`                                                                                                         | `{issues:[{type:`missing_slot                  | missing_link                                            |
| template.instantiate    | 将模板插入当前项目；支持在某页前/后/末尾追加；可**锁死 Slot 几何**避免走样       | `templateId`，`insert`:{`mode`:`before                                                                                | after                                          | append`，`referencePage`?}，`lockSlots?`(bool)            |
| template.map_slots      | 若自动识别失败，手工/半自动映射 Slot 与框体                         | `pageIndex`，`mapping`:[{`slotKey`,`frameId`}]                                                                        | `{mapped:[{slotKey,frameId}]}`                 | `list_page_items / get_page_content_summary`            |
| template.bind_data      | 绑定结构化数据（JSON）到 Slot Key                           | `pageIndex                                                                                                           | range`，`data`(object)                          | `{boundKeys:[…]}`                                       |
| template.fill           | 写入**单一 Slot**内容（文本/图片/表格）                         | `pageIndex`，`slotKey`，`content`：`text{value, styleRole?}` \| `image{filePath,fitMode}` \| `table{data[][], header?}` | `{applied:true, frameId}`                      | `create_text_frame / place_image / populate_table`      |
| template.fill_batch     | 按映射批量填充                                           | `pageRange`，`items`:[{`slotKey`,`content`…}]                                                                         | `{applied:n}`                                  | 同上                                                      |
| template.update_variant | 切换模板**变体**（如封面 A/B，图片墙 3×N/4×N），保持已填数据            | `pageRange`，`variant`(string)                                                                                        | `{variant, remapped:n}`                        | `duplicate_page / move_page / set_page_item_properties` |
| template.apply_master   | 应用模板内的主版面到指定页                                     | `templateId`，`masterName`，`pageRange`                                                                                | `{appliedPages:[…]}`                           | `apply_master_spread`                                   |

### A2. 主题与样式（3 个）

| 工具名              | 用途 / 智能提示                       | 主要 JSON 字段                                                                                   | 返回 / 反馈          | 合并自                                                                                                  |
| ---------------- | ------------------------------- | -------------------------------------------------------------------------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------- |
| theme.set        | 设定/覆盖模板主题 Token（字体层级/色板/基线网格）   | `typography`:{H1/H2/Body/Caption…}，`colors`:{primary,accent,…}，`baseline`:{increment,offset} | `{theme}`        | `create_paragraph_style / create_character_style / create_color_swatch / set_document_grid_settings` |
| style.apply_role | 以角色（H1/H2/Body/Caption 等）快速套用样式 | `target`:{`frameId` 或 文本范围}，`role`，`overrides?`                                              | `{applied:true}` | `apply_paragraph_style / apply_character_style`                                                      |
| swatch.create    | 新建色板（若主题未覆盖）                    | `name`，`rgb`{r,g,b}                                                                          | `{swatchName}`   | `create_color_swatch`                                                                                |

### A3. 素材与链接（2 个）

| 工具名             | 用途 / 智能提示                          | 主要 JSON 字段                    | 返回 / 反馈            | 合并自                       |
| --------------- | ---------------------------------- | ----------------------------- | ------------------ | ------------------------- |
| assets.register | 预登记素材键值（`hero`, `site_plan`…）供批量填充 | `assets`:[{`key`,`filePath`}] | `{registered:[…]}` | ——                        |
| assets.check    | 检查断链/大图警告/色域                       | `scope`:`doc                  | pageRange`         | `{links:[{path,status:`ok |

---

## B. 项目/页面与原子化灵活操作（可少用，但要有）

### B1. 项目 IO & 导出（3 个）

| 工具名          | 用途 / 智能提示             | 主要 JSON 字段  | 返回 / 反馈                                 | 合并自                                                                           |
| ------------ | --------------------- | ----------- | --------------------------------------- | ----------------------------------------------------------------------------- |
| project.io   | 统一的项目 IO：`new         | open        | save                                    | close`                                                                        |
| doc.info.get | 获取文档概要（页数、样式/色板计数、图层） | 无           | `{pageCount, styles, swatches, layers}` | `get_document_info / get_document_styles / list_color_swatches / list_layers` |
| doc.export   | PDF/图像/EPUB/打包（统一）    | `type`:`pdf | images                                  | epub                                                                          |

### B2. 页面与图层（3 个）

| 工具名             | 用途 / 智能提示    | 主要 JSON 字段                                                                   | 返回 / 反馈        | 合并自                   |
| --------------- | ------------ | ---------------------------------------------------------------------------- | -------------- | --------------------- |
| page.manage     | 新增/删/复制/移动   | `action`:`add                                                                | delete         | duplicate             |
| page.background | 设背景（全幅矩形）    | `pageIndex`，`backgroundColor`，`opacity?`                                     | `{frameId}`    | `set_page_background` |
| page.guides     | 建竖/横栅格（可贴边距） | `pageIndex`，`rows,columns,rowGutter,columnGutter,fitMargins,removeExisting?` | `{guideCount}` | `create_page_guides`  |

### B3. 原子对象操作（7 个）

| 工具名               | 用途 / 智能提示              | 主要 JSON 字段                                                                                                | 返回 / 反馈                                                             | 合并自                                                                           |
| ----------------- | ---------------------- | --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| text.upsert       | 新建或更新文本框（可直接写内容）       | **创建**:`pageIndex,x,y,width,height`；**更新**:`frameId`；通用：`content?`，`alignment?`，`styleRole?`              | `{frameId, edited:true}`                                            | `create_text_frame / edit_text_frame`                                         |
| image.place       | 放置/重链图片（支持自适应）         | `pageIndex                                                                                                | spreadIndex`，`filePath`，`x,y,width?,height?`，`fitMode`，`linkImage?` | `{frameId, linkPath}`                                                         |
| table.upsert      | 创建或填充表格                | **创建**:`pageIndex,x,y,width,height,rows,columns,headerRows?`；**填充**:`tableId                              | tableIndex,data[][]`                                                | `{tableId,cellsUpdated}`                                                      |
| selector.by_label | 通过 label/类型/样式选择对象（更稳） | `scope`:`page                                                                                             | spread                                                              | doc`，`where`{`type?`:`text                                                    |
| item.transform    | 移动/缩放/旋转/透明度           | `target`:{`itemId` 或 `pageIndex+itemIndex`}；`move?`{dx,dy}；`resize?`{width?,height?}；`rotate?`；`opacity?` | `{itemId,bbox}`                                                     | `move_page_item / resize_page_item / set_page_item_properties / reframe_page` |
| item.group        | 组/解组/增删组内成员            | `action`:`group                                                                                           | ungroup                                                             | add                                                                           |
| hyperlink.create  | 目录/跳转/外链               | `source`:`frameId                                                                                         | findText`，`destination`:`pageIndex                                  | url`，`linkType`                                                               |

---

## C. 质检、协作与兜底（5 个）

| 工具名                | 用途 / 智能提示                     | 主要 JSON 字段           | 返回 / 反馈                                          | 合并自                                       |
| ------------------ | ----------------------------- | -------------------- | ------------------------------------------------ | ----------------------------------------- |
| qc.lint            | 一键预检：溢出文本/断链/缺字库/未使用色板等       | `scope`:`doc         | pageRange`，`rules?`                              | `{issues:[{type,target,severity, hint}]}` |
| ops.review.request | 提交人审任务（生成所选页低清预览并附问题清单）       | `pageRange`，`notes?` | `{taskId, previews:[{page,imagePath}], issues?}` | `export_images`（低清）                       |
| ops.comment        | 针对页/对象添加批注（可@人）               | `target`:`page       | itemId`，`text`，`assignee?`                       | `{commentId,time}`                        |
| script.run         | **ExtendScript 兜底**：当无等价工具时调用 | `code`(string)       | `{stdout,stderr?}`                               | `execute_indesign_code`                   |
| help.tools         | 返回工具 Schema 与示例（便于 Agent 自检）  | `name?`              | `{tools:[{name,schema,examples}]}`               | `help`                                    |

> **总计 34 个工具（≤40）**。其中 **模板驱动 16 个**（A1+A2+A3=16），**项目/页面/原子操作 13 个**（B 组），**QA/协作/兜底 5 个**（C 组）。

---

### 运行建议（落地实践）

1. **人类先行**：在 InDesign 中创建 3–6 套模板（封面/目录/单图+说明/图集 N×M/分析图/指标表/结尾页），统一用对象 **Label 命名 Slot**（如 `title`, `subtitle`, `hero_image`, `metrics_table`）。

2. **注册与体检**：`template.register → template.validate → template.preview`，修复报错项（断链/缺字库/未标 Slot）。

3. **实例化与填充**：`template.instantiate(lockSlots:true) → template.map_slots(必要时) → assets.register → template.bind_data → template.fill_batch`。

4. **质检与导出**：`qc.lint → assets.check → doc.export(type:pdf/package)`；若遇到无法覆盖的需求，`script.run` 注入 ExtendScript；必要时 `ops.review.request` 拉人兜底。

5. **最小原子化**：仅在“非常规页”使用 B3 原子工具（`text.upsert`, `image.place`, `table.upsert`, `item.transform` 等）微调。

---

> **与原 120+ 工具的映射说明**：上表“合并自”列给出了关键对照，确保现有 MCP Server 可在不破坏兼容性的前提下合并为稳定、低熵的调用面。完整原始工具清单与字段定义可参见你提供的 InDesign MCP Server 文档。

如果你希望，我可以把以上 Schema 直接整理为一份 `OpenAPI/JSON Schema` 草案或生成一份“Agent 使用手册”页内速查卡。
